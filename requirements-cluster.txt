# Cluster / training requirements — the self-contained VST-free dependency split
# (issue #20, finalizing the stub left by the #22 training harness).
#
# This is the COMPLETE set to install on the GPU cluster: the base scientific
# stack MINUS the eval/dashboard packages, PLUS the training-only libs. Training
# is VST-free for every family except SynthRLi (D-RL-RENDER) — hence dawdreamer.
# pyloudnorm / streamlit stay out: eval + dashboard only, both local (D-EVAL).
# librosa IS needed: the preset-gen-vae families build their mel filterbank with
# librosa.filters.mel at fit time (models/presetgen_vae/network.py). PyTorch
# Lightning is imported only under models/training/; pyyaml only by
# TrainingConfig.from_yaml. Neither is in the base requirements.txt, so the Mac
# eval path stays Lightning-free (D-FRAMEWORK).
#
# Versions are pinned to the local dev environment (Python 3.11) for reproducible
# cluster training runs. On Linux, `pip install torch==...` pulls the CUDA wheel
# automatically. See cluster/README.md for the conda + pip install walkthrough.

# Base scientific stack (config.py + dataset.torch_dataset import these directly).
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
python-dotenv==1.2.2
torch==2.12.1
librosa==0.11.0  # mel filterbank for the preset-gen-vae front-end (fit-time)

# In-training rendering — SynthRLi only (D-RL-RENDER). Version verified by the
# D-SELFDESC spike; also needs Dexed 0.9.8 on the node (see cluster/README.md).
dawdreamer==0.8.3

# Training-only (models/training/): Lightning harness + YAML config parsing.
lightning==2.6.5
pyyaml==6.0.3

# Experiment tracking (opt-in via `logger.wandb: true`; models/training/trainer_factory).
# API key + online/offline mode come from the WANDB_* env (see cluster/cluster.env.example).
wandb==0.28.0
