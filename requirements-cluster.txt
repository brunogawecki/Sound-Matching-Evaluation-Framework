# Cluster / training requirements — the self-contained VST-free dependency split
# (issue #20, finalizing the stub left by the #22 training harness).
#
# This is the COMPLETE set to install on the GPU cluster: the base scientific
# stack MINUS the VST/render/eval/dashboard packages, PLUS the training-only libs.
# Training reads self-describing corpora and never touches a live plugin
# (D-SELFDESC / D-EVAL), so none of dawdreamer / librosa / pyloudnorm / streamlit
# is needed. PyTorch Lightning is imported only under models/training/; pyyaml only
# by TrainingConfig.from_yaml. Neither is in the base requirements.txt, so the Mac
# eval path stays Lightning-free (D-FRAMEWORK).
#
# Versions are pinned to the local dev environment (Python 3.11) for reproducible
# cluster training runs. On Linux, `pip install torch==...` pulls the CUDA wheel
# automatically. See cluster/README.md for the conda + pip install walkthrough.

# Base scientific stack (config.py + dataset.torch_dataset import these directly).
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
python-dotenv==1.2.2
torch==2.12.1

# Training-only (models/training/): Lightning harness + YAML config parsing.
lightning==2.6.5
pyyaml==6.0.3
