"""Per-axis metric implementations.

``parameter.py`` holds the parameter-axis callables; ``audio_based.py`` holds the
audio-input callables (the magnitude, timbre, loudness, and pitch axes). The
perceptual (embedding) axis is deferred to potential future work and is not
implemented (see ``D-METRIC-PERCEPTUAL`` in ``docs/DECISIONS.md`` and issue #8).
"""
